#include <stdio.h>
#include <time.h>
#include <unistd.h>
#include <stdlib.h>

int main(void){
    printf("ルール指定した時間の間に画面にでた■の数の合計を入力してください\n");
    printf("その数に近いほどもらえる点数が多くなります。\n");
    getchar();

    // 画面を消去するコマンドを実行
    // Windowsの場合は "cls"、それ以外（Mac/Linux）は "clear"
    #ifdef _WIN32
        system("cls");
    #else
        system("clear");
    #endif

    int a,b;
    srand((unsigned int)time(NULL));

    printf("時間を指定してください（秒）:")
    scanf("%d",&a);
     b=rand()% 42+9;
    for (int i = 0; i < b; i++) {
        printf("■");
        if(i%10=0){
            printf("\n\n");
        }
    }
    fflush(stdout);
    sleep(a);
    printf("■の数を入力してください：");
    scanf("%d",&int c);
    int d=b-c
    if(d<0){
        d=d*-1
    }else{

    }
    printf("\nあなたの点数は%dです。",d);




   
   
    return 0;

}  
    

